// Classe que representa o modelo de entrada com o prompt
class InputPrompt {
    // O construtor recebe um objeto com a propriedade 'prompt'
    constructor({ prompt }) {
      // Inicializa a instância com o prompt fornecido
      this.prompt = prompt;
    }
  }
  
  // Exporta a classe InputPrompt para uso em outros arquivos
  module.exports = InputPrompt;
  


